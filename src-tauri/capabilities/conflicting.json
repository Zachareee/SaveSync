{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "conflicting",
  "description": "Capability for the conflicting window",
  "windows": [
    "/conflicting/*/*"
  ],
  "permissions": [
    "core:default",
    "dialog:default",
    "core:window:allow-destroy"
  ]
}
