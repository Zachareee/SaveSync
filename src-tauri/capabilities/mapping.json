{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "mapping",
  "description": "Capability for the mapping window",
  "windows": [
    "/mapping"
  ],
  "permissions": [
    "core:default",
    "dialog:default",
    "core:window:allow-destroy"
  ]
}
